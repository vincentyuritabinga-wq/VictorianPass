@ When an admin rejects a proof of payment, display the rejection reason/message in the request details. Add an Edit button that allows the admin to update the rejection message if the user submits a new or updated proof of payment, without changing the existing UI.




@ After the user’s uploaded payment receipt is verified:

The uploaded receipt should automatically move to the "View Details" section.

The receipt must be displayed as an image, representing the user’s proof of payment.

The transition should be smooth (e.g., slide or fade animation) to indicate the receipt is being moved.

The receipt image should be properly scaled to fit within the "View Details" container without distortion.

Clicking the receipt image should allow the user to view it in full size or download it..




@ Enforce validation for Whole pool (exclusive) bookings when available slots are insufficient (e.g., only 17 people available):

Show a clear warning message when the option is selected.

Prevent the user from proceeding to the next step while this condition is invalid.

Disable or block the Next / Continue button until the user changes to a valid booking option or slot availability meets the requirement.
This validation must be enforced even if the user dismisses or ignores the warning.

@ Change the “Update Proof” button color to be distinct, while keeping the same button style as the others.

@ Make sure in the account pages they can click the Victorian Pass logo and it will take them into the mainpage

@ Improve the collapsed accordion row on the Dashboard Profile page of Resident and Visitor

so that the title displays only: “Reservation – Community Pool” (using the amenity name). Remove any extra or redundant text.

@ When an admin clicks a notification indicating that a user canceled a reservation, automatically redirect the admin to the Archived Requests section.
Ensure the navigation is consistent with existing admin routing and behavior.

@ Update the downpayment back button to match the exact design, styling, and behavior of the reserve page back button.
Keep the existing layout and CSS structure unchanged.